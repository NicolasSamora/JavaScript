//Pode armazenar diferentes tipos de dados (string, number, boolean, etc).

// Exemplo: dados de uma pessoa (mistura tipos diferentes)
let pessoa = {
    nome: 'Lucas',
    idade: 19,
    estudante: true
};

console.log(`Nome: ${pessoa.nome}`)
console.log(`Idade: ${pessoa.idade}`)
console.log(`Estudante: ${pessoa.estudante}` ? 'sim' : 'não')

